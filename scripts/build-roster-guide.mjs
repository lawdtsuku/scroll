import {readFile,writeFile} from 'node:fs/promises';
const root=new URL('../',import.meta.url);
const guide=await readFile(new URL('ROSTER-AND-SNAPSHOTS.md',root),'utf8');
await writeFile(new URL('dist/roster-guide.js',root),'// Generated by scripts/build-roster-guide.mjs; edit ROSTER-AND-SNAPSHOTS.md.\nexport const ROSTER_GUIDE='+JSON.stringify(guide)+';\n');
await writeFile(new URL('dist/dm-template-kit/ROSTER-AND-SNAPSHOTS.md',root),guide);
